@model IEnumerable<Vidly.Models.Customer>
@{
    ViewBag.Title = "Viewers";
}

<h2>Viewers</h2>
<p>
    @Html.ActionLink("Add Viewer", "AddViewer", "Viewers", null, new { @class = "btn btn-primary mb-3" })
</p>
@*<div id="customers">
        @foreach (var viewer in Model)
        {
            <div class="card text-white bg-secondary mb-3">
                <div class="card-body">
                    <h4 class="card-title">
                        @Html.ActionLink(viewer.Name, "Details", "Viewers", new { id = viewer.Id }, new { @class = "pr-3" })
                        @Html.ActionLink("Edit", "EditViewer", "Viewers", new { id = viewer.Id }, new { @class = "badge badge-primary mr-3" })
                        <button data-customer-id="@viewer.Id" class="badge badge-danger js-delete">Delete</button>
                    </h4>
                    <p class="card-text">
                        Payment Plan: <br />
                        @viewer.MembershipType.Name
                    </p>
                </div>
            </div>
        }
    </div>*@

<table id="customers" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Customer</th>
            <th>Membership Type</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("#customers").DataTable({
                ajax: {
                    url: "/api/customers",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, customer) {
                            return "<a href='/viewers/editviewer/" + customer.id + "'>" + customer.name + "</a>";
                        }
                    },
                    {
                        data: "membershipType.name"
                    },
                    {
                        data: "id",
                        render: function (data) {
                            return "<button class='btn-link js-delete' data-customer-id=" + data + ">Delete</button>";
                        }
                    }
                ]
            });
            $("#customers").on("click", ".js-delete", function () {
                var button = $(this);
                bootbox.confirm("Are you sure you want to delete this user?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/api/customers/" + button.attr("data-customer-id"),
                            method: "DELETE",
                            success: function () {
                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }
                });
            });
        });
    </script>
}